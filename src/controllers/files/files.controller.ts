import { Request, Response } from 'express';
import { uploadFile } from 'utils/libs/fileFactory/uploadFile';

export class FilesController {
	/*	upload = async (req: Request, res: Response) => {
		try {
			const file = uploadFile(req, res);
			console.log(file);
		} catch (e) {
			res.send(e);
		}
	};*/
}
